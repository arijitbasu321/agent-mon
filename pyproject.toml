[project]
name = "agent-mon"
version = "0.1.0"
description = "System monitoring agent powered by Anthropic Agent SDK"
requires-python = ">=3.10"
dependencies = [
    "claude-agent-sdk>=0.1.0",
    "psutil>=5.9.0",
    "docker>=7.0.0",
]

[project.scripts]
agent-mon = "agent_mon.cli:main"
